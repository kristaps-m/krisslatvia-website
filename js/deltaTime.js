// Common helper for delta-time based animations
// Usage: call this at the start of your animation loop
// var lastTime = performance.now();
// function animate(time) {
//   var deltaTime = (time - lastTime) / 1000; // seconds since last frame
//   lastTime = time;
  
  // Now use deltaTime for all movement:
  // position += speed * deltaTime; // where speed is in units per second
// }
